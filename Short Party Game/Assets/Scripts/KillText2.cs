using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KillText2 : MonoBehaviour
{
    // Use this for initialization
    void Start()
    {
        Destroy(gameObject, 12);
    }

    // Update is called once per frame
    void Update()
    {

    }
}
